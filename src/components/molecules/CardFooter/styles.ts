import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  footer: {
    padding: 30,
    backgroundColor: '#eee',
    borderRadius: 4,
  },

  annotation: {
    fontSize: 12,
    color: '#333',
  },
});

export default styles;
